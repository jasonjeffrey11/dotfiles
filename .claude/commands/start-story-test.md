Together we are going to setup the project ready to work on a new story and then create a plan to work on it. Follow the list below STRICTLY in order and only ask me 1 question at a time.

1. create a new worktree in the ./.worktrees folder with $ARGUMENTS as its branch name and staging as it's base. ASK ME before creating the worktree. Also  YOU MUST make sure staging is up to date with origin before creating the new branch.
2. use summoner to find an available dev environment and offer the option for me to claim one. ONLY offer envs for CVR and DO NOT offer staging or intergration. also DO NOT claim an enviroment without asking me.
3. make sure 
4. we now need to get the correct environment variables for the dev env we are going to be working against. run the script write_env_config with dev env name we chose in the previous step otherwise ask me for an env to use
5. run 'npm install' to make sure all packages are up to date 